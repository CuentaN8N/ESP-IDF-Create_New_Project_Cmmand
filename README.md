# ESP-IDF-Create_New_Project_Cmmand
I wanted to simplify the process of creating new code in ESP-IDF CLI, so I made a python program which, with just asking the name of the new program, creates it in a new folder. 
